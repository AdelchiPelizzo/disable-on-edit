<!--
 - Created by Adelchi on 17/09/2019.
 -->


<aura:component description="modalFooter" implements="force:hasRecordId">
    <force:recordEdit aura:id="saveRecord" recordId="{!v.id}"/>
    <aura:attribute name="id" type="String"/>
    {!v.id}
<!--    <lightning:button name="cancel" label="Cancel" onclick="{!c.handleCancel}"/>-->
    <lightning:button name="Save" label="Save" variant="brand" onclick="{!c.save}"/>
</aura:component>
